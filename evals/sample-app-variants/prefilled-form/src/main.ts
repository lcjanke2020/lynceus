// prefilled-form variant — a static preferences form whose controls start in a
// known non-empty state: a pre-filled "Display name" input, two pre-checked
// boxes (Email updates, Beta features), and a plan radio group defaulting to
// "Free". Used by the clearing-fill and idempotent-toggle L4 scenarios, which
// read the final state via get_form_state. No runtime behavior is required —
// the initial markup is the fixture.
export {};
